# 第二讲作业：Agent 工作规则

## 项目目标与工作入口

- 在本目录完成《当代量化交易系统的原理与实现》第二讲的源码学习、CTP 终端 Demo 和因子研究，验证后再上传 GitHub。
- 用户已注册 SimNow、有 GitHub 账号；当前选择终端脚本，不配置完整交易图形界面。
- 每次继续工作先读 `wiki/index.md`、`wiki/project-status.md` 和 `wiki/log.md` 的最近记录。实际文件和运行证据优先于旧状态说明。
- 仅使用用户当前提供的个人 SimNow 配置，禁止回退到示例或他人账号；旧账号配置、会话与运行证据不保留。
- 当前任务范围以用户最新请求为准。阶段规划不能冒充已实现的功能。
- `llm-wiki.md` 是知识库方法原文；本文件是本项目具体维护规则。`Agent.md` 仅作入口，不维护第二份规则。

## 目录职责

- `raw/course/`：课程原件；`raw/web/`：带 URL、获取时间及删节说明的网页资料快照。
- `raw/repos/`：上游源码的本地学习副本。保持原样，不把作业代码写入上游；记录来源 URL 和 Git 提交号。
- `wiki/sources/`：阅读范围、源码摘要与证据；`wiki/concepts/`：面向初学者的概念解释；`wiki/code/`：实现设计与调用链。
- `projects/ctp_demo/`：自己的 CTP 程序；`projects/factor_research/`：自己的因子研究。
- `configs/`：无凭据的配置示例；`configs/local/`：仅本地的账号配置。
- `data/`：本地研究数据；`reports/`：可核验报告与精选证据；`reports/local/`：未经脱敏的运行输出。
- `scripts/`：可复用的项目维护工具。保留用户现有 `.obsidian/` 配置。

## 资料消化与问答

1. 阅读相关原始资料及实际源码，不凭项目名称推断实现。
2. 新资料记录来源、日期、版本；源码结论给出文件、类/方法及固定 commit 链接。
3. 中文解释先说作用，再用具体数据或调用过程说明。保留准确的英文标识符。
4. 明确区分“源码事实”“课程要求”“教学示例”“设计建议”“待验证”。类名出现不等于读懂或执行过该模块。
5. 重要问答写回相应笔记，更新相关页面链接和 `wiki/index.md`；向 `wiki/log.md` 追加记录。
6. 发现新旧资料冲突时说明版本及差异，更新结论；原始资料保持不变，不把 AI 笔记当作原始证据。
7. 定期检查相对链接、未被索引的页面、过时状态和未解决问题。不要为凑页数生成空泛笔记。

## 实现与证据

- 优先复现老师的顺序：源码学习 → 底层 MdApi 行情 Demo → TdApi 和价格触发设计/实现 → 因子研究。
- 安装前核对本地源码版本、Python 和 Mac arm64 依赖；源码下载、库安装、可导入、联网成功是不同阶段。
- CTP 练习使用 SimNow 仿真环境。发单前明确合约、方向、开平、数量、触发规则、委托价和重复触发行为，依据用户授权执行。
- 请求返回值、订单受理、实际成交分别记录。连接断开或状态不明时不能把“没收到回报”当作“没有成交”。
- 必要验证应覆盖有意义的边界条件；模拟数据只能证明模拟行为，不能代替真实联网或成交记录。
- 因子研究先写 clue、信息可用时点、数据区间和评价方法，再运行实验；保留失败候选和样本外结果，不把回测表现等同未来收益。
- 本次股票因子研究以用户提出的“均线/趋势是否预示股票自身上涨”为 clue；同一批候选同时检验绝对未来收益（主评价）与同日横截面排序（辅助评价）。两套结果一并报告，不看结果后更换主目标。
- 每轮预先列出 10–20 个候选，最多 4 轮。每轮保留完整候选、计算口径、结果和淘汰理由。训练和验证段用于筛选及调整下一轮探索方向，因此验证段不是完全独立的最终证据；最终测试段只在固定选择和规则后查看一次。上一轮已看过的黄金测试数据不用于本次股票选择。
- 股票研究先应用**当时的**历史成分，并检查归一化价格、复权、零成交量和未来可交易性。信号由 t 日收盘形成，不能假设在 t 日收盘成交；两种评价须共用未来收益和时间切分。不得用今天成分或老师 harness 未过滤的 `result_df` 冒充历史成分回测。
- 绝对收益区分“预测统计”“假设成交的策略回测”；缺少真实价格、复权或停牌/涨跌停状态时，明确限制，不能声称已经验证可交易利润。相关股票、相邻持有窗口并非独立样本。
- 只有在主评价与辅助评价均有稳定的训练/验证表现、最终测试没有明显失效、并能说明成本和交易限制时，才考虑把结果称为“理想”并制作 HTML 幻灯报告；否则交付清晰的 Markdown 报告和复现证据。
- **新一轮（2026-09-24）**：用户再次指定“长短期均线关系”线索。先分析上轮失败：简单均线强弱在训练段和验证段表现不一致，回归加成交量在 2021—2023 年的绝对收益检验失败。新候选应研究均线距离、方向变化、持续时间与市场状态的条件关系，注明彼此相似性；每轮 10–20 个，最多四轮。
- 上轮的 2021—2023 年测试结果已经公开，不能重新包装为本轮全新样本外测试。若可获得一致口径的 2024 年以后股票行情，应先固定股票池与评价规则，再作真正向前检验；若得不到新数据，本轮明确标为历史探索，不对外宣称发现通过独立测试的因子。训练/验证信息可用于迭代，但必须保存全部候选和失败结果。
- **继续研究（2026-09-24，用户本次请求）**：先去掉以上两轮代码中依赖未来成交量、未来异常价格的入样筛选，保留旧报告为历史记录，不覆盖旧结果。当前 `recovering_above_ma20` 的 2024—2025 年结果已被查看，该区间今后只算开发/验证资料，不再称为全新测试。当前目标仍是股票自身未来涨跌，横截面排序作为辅助；“效果理想”须事先定义，不能在看结果后降低门槛。
- 新实验先固定信号日可知的股票池、行情质量与资格条件；对未来未成交、停牌或缺失回报分别计数，并在主要意向发单统计中保留这些信号，不因未来是否顺利成交或收益是否极端而删除样本。预测统计与可交易收益分开。必须检查数据源的前复权口径、涨跌停和 ST/停牌字段的可得性，不把无法验证的执行条件说成已验证。
- 小范围比较中长期趋势强度、短期回撤深度及交互作用；若失败，再在同一均线 clue 下换角度。每轮预列 10—20 个候选，最多 4 轮，保留完整候选及失败理由。下一份真正向前测试以未看过的 2026 年行情为主；测试前锁定公式、股票池、评价与成本假设，仅开封一次。测试期短或结果不确定时如实报告，不能为获得理想结论增加第五轮、反复开封或把旧区间重命名为独立测试。
- 执行后记录版本、命令（无凭据）、输出路径、结果和限制。未运行就写“未运行”，失败就保留原因。
- **统一数据延伸（2026-09-24）**：用户取得同源 BaoStock 历史数据后明确要求继续均线 clue。前次四轮研究已结束，不把本次写成其第五轮；本次单独预列一批 12 个有机制差异的均线候选，先核对各信号日的精确历史成分，再计算。按全市场交易日每隔六日取样，`t` 日收盘形成信号，预测 `t+1` 开盘至 `t+6` 开盘的五日收益；股票自身涨跌为主，同日横截面排名为辅，二者同规则报告。沿用信号日可知资格、未来无成交不事后删样本、0.3% 仅为成本敏感性假设。2008—2026 年（包括已经看过的 2026 年）全部属于已知历史探索；即使换成统一数据或动态成分，也不能将任何区间称为新的独立测试或认定理想／可交易因子。保留全部候选及失败结果，下一次独立检验须等未查看的新时段。
- **全交易日稳健性审计（2026-09-24）**：固定此前已选的 `market_down_day` 公式、2023 年末 300 股股票池及 `t+1` 至 `t+6` 收益定义，对 2024、2025、2026 年分别统计全部合格交易日，不按六日抽样起点择优。相邻五日收益重叠，须按完整交易日序列用 10 日和 20 日移动区块评估均值和相对收益差的不确定性；这是已看过历史的事后诊断，不生成新候选，也不作为独立测试或策略盈利证据。
- **多均线与价格行为候选（2026-09-24）**：用户询问是否已研究多条均线的联合关系及与其他价格指标的组合。本次只在 `wiki/concepts/factor-research.md` 记录十个明确定义、尚未运行的候选；旧四轮上限和已查看数据的性质不变。设计稿不是第五轮、实验结果或预先锁定的新样本外检验。若以后另开实验，须先固定股票池、数据时段、主评价与成本／执行假设，再运行并保留全部候选。
- **新研究：多均线＋价格／成交量（2026-09-24，用户明确授权）**：此前四轮和统一数据延伸均已结束；从本条起单独计数，最多四轮、每轮预列 10—20 个候选。第一轮使用概念页已记录的十个多均线与开高低收组合，不因结果删候选；后续轮次只在上一轮完整结果记录后预列公式。新研究沿用同源 BaoStock 日线与信号日准确沪深300历史成分，`t` 日收盘后打分，`t+1` 开盘至 `t+6` 开盘的五日自身收益／上涨比例为主；同日股票池收益差和 Rank IC 为辅助，不作为单独否决理由。前复权价格与假设往返成本 0.3% 只支持预测／敏感性分析，缺涨跌停执行证据不能称为可交易净收益。所有 2008—2026 年日期已被先前研究查看，故本研究对这些日期只能称历史探索／复核，不能称新的独立样本外测试；可另取未用于选型的股票群作跨股票池转移检查，但不能把它冒充未来时期。效果“理想”须有事先固定的规则、足够信号、扣成本后有可信的正收益与新时段检验；未达到便如实报告，不更改门槛追求漂亮结果。
- **新研究第二轮方向**：第一轮仅同步扩大多层均线的分数通过宽松历史初筛，但中证500锁定转移检验在 2024、2026 年为负；该固定中证500样本已开封，后续候选不能再拿它做新的择优测试。第二轮只在原沪深300探索资料中，把同一同步扩张条件与不过度追高、收盘位置、当日阳／阴线、放量／缩量、真实波幅扩／缩、跳空方向配对比较。先列满 10—20 个不同候选并保存公式，再运行；不能因为知道中证500失败而只保留看起来漂亮的变体。
- **新研究第三轮方向**：第二轮“同步扩大＋向上跳空”在历史沪深300三段均值较高，但近段上涨比例仍不足一半，三段扣假设成本后的区块区间不能同时支持稳定正收益。第三轮把“跳空本身”“均线排列”“间距同步扩大”作正反对照，再检验跳空当天阳／阴线、放／缩量、突破／未突破、跳空相对真实波幅大小；预列 10—20 项并完整保留。此轮仍只用已看过的沪深300探索资料，不能重复使用中证500当独立验证。
- **新研究第四轮方向**：第三轮对照显示向上跳空本身及仅叠加均线排列近年均弱，叠加同步扩大后历史均值提高；阴线、放量、未突破等组合有较好均值，但每一项在三段的区块区间至少一段未能排除扣成本后为负，极端跳空样本尤其少。最后一轮只在同一旧沪深300资料中预列 10—20 项，把同步扩大＋跳空与不过度追高、阴线／收盘位置、放量／适中量、未突破的两两组合与反向对照；保留空结果与稀疏样本，不因结果再加第五轮或重复打开中证500。

## 本地与提交

- 本阶段在本地工作；用户决定上传时再发布到 GitHub。
- 当前任务只整理并检查待提交文件；不执行 `git commit`、`git push` 或创建 GitHub 仓库/PR，等用户手动提交。
- 密码、验证码、个人账号配置不进入 Wiki、README、公开日志或 Git。示例只用空值/占位符。
- `raw/repos/`、本地环境、原始行情及个人配置默认不纳入作业提交；保留上游版本清单和复现说明。
- 尊重资料许可，引用上游代码保留来源；不把同学的完成声明当作老师的评分标准。

依据：课程 Issue #2、项目内 `llm-wiki.md`、用户在本任务中的要求。
